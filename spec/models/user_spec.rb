require 'rails_helper'

RSpec.describe User, type: :model do
  # before (:all) do 
  #     @user = User.create(email: "test@example.com", username: "test", password: "password")
  #   end
  #   it 'checks that a user can be created' do
  #     expect(@user).to be_valid
  #   end
  #   it 'checks that a user can be read' do
  #     expect(User.find_by(username: "test")).to eq(@user)
  #   end
  #   it 'checks that a user can be updated' do
  #     @user.update(:email => "test1@example.com")
  #     expect(User.find_by_username("test")).to eq(@user)
  #   end
  #   it 'checks that a user can be destroyed' do
  #     @user.destroy
  #     expect(User.find_by(username: "test")).to be_nil
  #   end
end
